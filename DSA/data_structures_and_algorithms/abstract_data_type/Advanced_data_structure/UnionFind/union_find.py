from collections import defaultdict


class UnionFind:
    def __init__(self):
        self.groups=defaultdict(list)
    def find(self):
    def checkIfInsameGroup(self, a,b):#check
        for

    def union(self):# merge

