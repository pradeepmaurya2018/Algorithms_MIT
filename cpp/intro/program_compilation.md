a program written in c++ is compiled in following manner
1. compiler does a systax and scemantics check 
2. compiler converts cpp code in assembly code 
2. assembler converts that code into machine code i.e 0s and 1s 
3. linker links any included library and finally combined everything and creates an exe file
   1. there are two types of linking 
      1. statis linking 
      2. dynamic linking- executable only holds the reference to the libreary
      3. 