# How should you display and simple and
# What is a tiime table to watch
# what makes it so great
---
1. Simple and
2. Cretive thought was once given for this
``` python
def main():
    print("I am a function and you could write on that")
class SimpleClass:
    def functionDefinton(self):
        name=Thinking()
        thread=threadng.Thread()
        thread.start()


```
| Component | Responsibility | Notes |
|----------|----------------|-------|
| API      | Handle requests| Auth, rate limit |
| Service  | Business logic | Stateless |
| DB       | Data storage   | Indexed |
|New tables;kdjf sdlflsdaflsdf| sjsdkfk sddfhjjsdfh sdfsldflh||
|sldjf sdljflsdnfljsdlfllsdflsdorewo

```mermaid
flowchart LR
    User[name s this and something]--> API-->|This is making an attempy to clla this |Service-->Database
    Service-->Cache
    API-->API-Servce
    User1-->Data1
    API["TRhis is a note for API"]
    subgraph left
    Note1["This s where retry happens"]
    Node2[""II amds somethinhg slese ]
    end
    subgraph Right

     Note2[This is nod]
     end
    left-->Right
